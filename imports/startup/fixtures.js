import {
    Meteor
} from 'meteor/meteor';
import {
    Accounts
} from 'meteor/accounts-base';
import {
    Parties
} from '../api/parties/index';

// Initialize server
Meteor.startup(() => {
    // Loads initial values for DB
});
